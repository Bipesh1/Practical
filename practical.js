// This is a simple JavaScript program that adds two numbers
function add(a, b) {
    return a + b;
  }
  
  // Main program
  const num1 = 5;
  const num2 = 7;
  console.log(`Adding ${num1} and ${num2}: ${add(num1, num2)}`);